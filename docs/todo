Consider using atexit to make sure mem_free is always called when program ends
Add ability to profile using 'gprof'

Add statistics to mem.o, allow it to report total memory used etc

Let shapes store the inverse of their transformations so this isn't
calculated for every ray
